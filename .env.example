# API Configuration
# By default:
# - Development uses: http://api.9jacart.ng (to avoid SSL issues)
# - Production uses: https://api.9jacart.ng

# Override the default API URL if needed:
VITE_API_BASE_URL=https://api.9jacart.ng

# For local development with HTTPS (if you have valid certificates):
VITE_API_BASE_URL=https://api.9jacart.ng